Como correr 
1. navegar até src/main/java
2. compilar com javac -d ..\..\..\target\ -cp ..\..\..\target\lib\jsoup-1.18.3.jar search\*.java ---no linux-->  javac -d ../../../target/ -cp "../../../target/lib/jsoup-1.18.3.jar" search/*.java
3. correr
4. navegar ate target
5. java -cp ".;lib/jsoup-1.18.3.jar" search.Client  -----no linux--> java -cp ".:lib/jsoup-1.18.3.jar" search.Gateway

-> paginacao 10 a 10 -> paginar na fonte e nao no cliente
-> barrel abaixo deve guardar estado e deve ser sincronizado ao voltar
-> pelo menos 2 barrels
-> os downloaders escrevem para todos os barrels tem de ter os indices atraves de multicast fiavel (caso esteja algum indisponivel, dar retry)
-> dowloadrers com 2 ou mais thereads, pelo menos 2 downloads um em cada pc
->  2 barrels pelo menos (1 em cada pc)
-> gateway e client em pcs diferentes (apenas 1)

-> dias->barrel->downloader e cliente-> -------------------barrel e cliente linha dif----------- downloader ip na busca da queue
-> vazeta->barrel->downloader ->gateway->quuee->------gateway  e urlqueue ->linha dif com o meu ip

linux -> java -Djava.rmi.server.hostname="127.0.0.1" -cp ".:lib/jsoup-1.18.3.jar" search.Gateway
windows -> java "-Djava.rmi.server.hostname=192.168.1.7" -cp ".;lib/jsoup-1.18.3.jar" search.Gateway

usar os comandos acima na gateway e na queue



____________________________________________________________________________________________________________
_______________________________________META 2_______________________________________________________________

-------------------------------------------------------------------------------------------------------------


DIAS, as 3 pastas que nos devemos focar nesta meta estao dentro de  src/main/java/com/example/googol
os html alteram-se na pasta /src/main/resources/templates
temos um bug que se fizermos mvn install, o maven apaga o jsoup.jar e as stopwords mas é algo que nao presisamos de preocupar por agora
PARA PERCEBER MELHOR HACKER NEWS API -> VER DOCUMENTACAO https://github.com/HackerNews/API

A FAZER:
-> quando nao esta a ser processado o link ou seja quando os downloaders estao em baixo diz na mesma na mensagem que o link esta a ser processado
-> quando clico no botao para ir buscar a stories de hacker news fica naquela pagina até acabar a pesquisa, talvez seja melhor ir para a pagina inicial enquanto esse processo esta a acontecer
-> openai é um boa palavra a pesquisar e para testar hackernews


----------------!!!!!!!!----------------------------ATENCAO-------------------------------------------------------------------------
-------------------------------------------------------------------------------------------!!!!!!!!!!!!!-----------------------------------------

--------------------------------------------ATENCAO-------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------------------------------------
NOVOS COMANDOS E COMO CORRER ISTO:
TODOS OS TERMINAIS ABERTOS EM SD_proj/proj/
PARA CORRER :
mvn clean compile
Gateway -> mvn exec:java -Dexec.mainClass="search.Gateway"
queue -> mvn exec:java -Dexec.mainClass="search.URLQueueImpl"
downloader -> mvn exec:java -Dexec.mainClass="search.Downloader"
barrel -> mvn exec:java -Dexec.mainClass="search.Barrel" -Dexec.args="Barrel1"
ollama -> ollama run llama3
correr -> mvn spring-boot:run

NOTAS 
-> OS FICHEIROS JSOUP E STOPWORDS FORAM MOVIDOS PARA DATA

A FAZER 

-> MUITO LENTO A IR AS APIS
-> IP HARDCODED NOS COMANDOS ACIMA



----------------!!!!!!!!----------------------------ATENCAO-------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------------------------------------
--------------------------------------------ATENCAO-------------------------------------------------------------------------
-------------------------------------------------------------------------------------------!!!!!!!!-----------------------------------------